Docker-compose adds aliases so services can found them self.
create a network by project

Instead of down then up, do a diff to recreate only needed ones.
Better: warn the user and let it choose.

Subscribe to events.
